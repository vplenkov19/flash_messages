<% items.each do |item| %>
  <tr>
    <td><%= item.id %></td>
    <td><%= item.name %></td>
    <td><%= item.description %></td>
    <td>
      <%= link_to item, class: "notification-messages dynamic-form", remote: true, method: :delete, data: { container: 'items-container', type: 'json' } do %>
        <button class="btn btn-default btn-sm">
          <span class="fa fa-trash"></span>
        </button>
      <% end %>
    </td>
  </tr>
<% end %>